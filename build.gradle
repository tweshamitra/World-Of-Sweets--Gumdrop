apply plugin: 'java'
apply plugin: 'application'
mainClassName = "wos.GameRunner"

repositories {
    mavenCentral()
}

dependencies{
    testCompile 'junit:junit:4.12'
}

run {
	try {
		systemProperties System.getProperties()
		args System.getProperty("args").split()
	} catch (Exception ex) {
	}
}
